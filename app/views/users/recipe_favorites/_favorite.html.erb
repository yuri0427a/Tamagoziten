
<% if user_signed_in? %>
  <% if recipe.favorited_by?(current_user) %>
    <li class="bookmark_change_item">
      <%= link_to users_recipe_favorite_path(recipe), class: "bookmark-recipe-show", remote: true, method: :delete do %>
        <span><i class="glyphicon glyphicon-star" aria-hidden="true"></i>
        レシピ保存済<span>
      <% end %>
    </li>
  <% else %>
    <li class="bookmark_change_item">
      <%= link_to users_recipe_favorite_path(recipe), class: "bookmark-recipe-show", remote: true, method: :post do %>
        <span><i class="glyphicon glyphicon-star-empty" aria-hidden="true"></i>
          レシピ保存</span>
      <% end %>
    </li>
  <% end %> 
<% end %> 